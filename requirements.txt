streamlit>=1.38
requests>=2.31

# Only needed if you run the optional api.py REST companion (not required
# for Streamlit Community Cloud deploys, which only run app.py):
fastapi>=0.115
uvicorn>=0.30
